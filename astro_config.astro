#!/usr/bin/env astro

# This is my config file. Yay!
config =
    name = eventsmanager
    version = 0.1.0
    main = server/bin/server.js
    scripts =
        server:watch = astromon server/server.js
        test = mocha tests

# TOML
name = 'eventsmanager'
version = '0.1.0'
main = 'server/bin/server.js'

[ scripts ]
'server:watch':
