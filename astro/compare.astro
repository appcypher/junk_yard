#!/usr/bin/env astro

# October 14, 2018
fun compare(a, b):
	let compare = (0, 0)
	for x, y in zip(a, b):
		if x > y:
			compare[1] += 1
		elif x < y:
			compare[0] += 1
	return compare

compare([1, 3, 2], [3, 2, 1])

