#!/usr/bin/env astro

# Astro Dictionary
export {
    name: 'eventsmanager'
    version: '0.1.0'
    main: 'server/bin/server.js'
    scripts:
        'server:watch': 'astromon server/server.js'
        test: 'mocha tests'
    labels: [ 'events', 'smart contract', 'first, second and third' ]
    active: true
}

# TOML
name = "eventsmanager"
version = "0.1.0"
main = "server/bin/server.js"
labels = [ "events", ""]
active = true

[ scripts ]
"server:watch": "astromon server/server.js"
test: "mocha tests"

# Custom [everything is a string]
name = eventsmanager
version = 0.1.0
main = server/bin/server.astro
scripts =
    server:watch = astromon server/server.astro
    test = maestro tests
labels =
    events, smart contract, 'first, second and third'
active = true
