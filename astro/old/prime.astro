# Python
primes = lambda n: all(n % d > 0 for d in range(2, n))

# Rust
let primes = |n| (2..n).all(|d| n%d > 0)

# Astro
let primes = n => all(i mod d | i in 2..n)
let primes = n => (2..n).all(_ mod d)

